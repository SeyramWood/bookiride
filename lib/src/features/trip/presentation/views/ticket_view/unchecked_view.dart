import 'package:flutter/material.dart';

class UncheckedView extends StatelessWidget {
  const UncheckedView({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}