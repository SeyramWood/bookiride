import 'package:flutter/material.dart';

// const width = MediaQuery.sizeOf(context).width;
const vSpace = SizedBox(height: 10);
const hPadding = 23.0;
const vPadding = 15.0;
final borderRadius =  BorderRadius.circular(6);